# Dependencies
node_modules/
__pycache__/
*.pyc

# Logs and local data
*.log
feedback/
.env

# OS files
.DS_Store
Thumbs.db

# Local state and tests
state/
bin/
mega_sandbox*
mock_google*
.install_log


# Local development / upload dirs
BRANCH_Early_Release/

# Work in progress — not ready for the repo yet
# When V1.6 is ready, copy the relevant files out and commit them directly
Stabilize_core/
versions/

# Zips distributed via GitHub Releases only
*.zip

# Runtime logs — never commit
logs/
